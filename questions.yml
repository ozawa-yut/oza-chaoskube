questions:
- variable: defaultImage
  default: true
  description: "Use default Docker Image"
  label: Use Default Image
  type: boolean
  show_subquestion_if: false
  group: "Container Images"
  subquestions:
  - variable: image
    default: "quay.io/linki/chaoskube"
    description: "chaoskube image name"
    type: string
    label: chaoskube Image Name
- variable: dryRun
  default: true
  description: "don't kill pods, only log what would have been done‚ ‚ ‚ ‚ ‚ ‚ ˆŸ"
  type: boolean
  show_subquestion_if: false
  label: Dry Run Mode
  group: "Dry Run"
- variable: replicas
  default: "1"
  description: "number of replicas to run"
  type: string
  required: true
  label: number of replicas to run
  group: "Setting"
- variable: interval
  default: "10m"
  description: "interval between pod terminations"
  type: string
  required: true
  label: Interval
  group: "Setting"
